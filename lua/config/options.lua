--- User defined options that will override the defaults if there is any overlap.
--- Here is an example:

return {
  relativenumber = true,
  mouse = "a",
  grepformat = "%f:%l:%c:%m",
  grepprg = "rg --vimgrep",
}
